# AGENTS.md

## 1. 角色与原则
本文件是本项目内自动化开发与文档维护的总规则，优先级高于各 skill。

执行优先级（从高到低）：
1. 真实性：所有描述必须可由代码、配置、Git 历史或可验证文件支撑。
2. 可追溯性：每次文档更新必须给出时间、范围、依据。
3. 一致性：文档内容必须与真实项目结构一致，禁止保留过期描述。
4. 可执行性：规则应能直接落地为操作步骤，不写空泛建议。
5. 技术中立性：本文件只定义元规则与工作流，不写具体技术栈或实现细节。

## 2. 默认上下文
1. 新会话先读取 `doc/_instruction.md` 作为入口页。
2. 随后按任务再读取对应专题：核心理念与开发规范读 `doc/_instruction/core.md`，结构与接口读 `doc/_instruction/structure.md`，术语定义读 `doc/_instruction/glossary.md`。
3. 若 `doc/_instruction.md` 不存在，则回退读取 `Instruction.md`，必要时再迁回 `doc/_instruction.md`。
4. 任务涉及历史变化、经验复盘或最近改动时，额外读取 `doc/_devlog.md`。
5. 关键结论必须用 `find`、`rg --files`、`git log`、`git diff` 等真实文件或 Git 证据复核。
6. 任务执行遇到困难、原则冲突或决策不确定时，主动读取 `doc/_note.md` 看看以往的经验是否能给你启发。

## 3. 文档职责
1. `doc/_instruction.md`：项目说明入口页，只负责路由与阅读顺序。
2. `doc/_instruction/core.md`：核心理念、开发规范与当前边界。
3. `doc/_instruction/structure.md`：目录结构、模块职责、接口与运行契约。
4. `doc/_instruction/glossary.md`：术语表。
5. `doc/_devlog.md`：按时间追加的开发记录，只增不改历史。
6. `Instruction.md`：历史兼容文档，需与 `doc/_instruction.md` 保持一致或明确迁移状态。
7. `doc/_note.md`：沉淀的工作经验、原则与启发点。

## 4. 模式与权限
1. 模式词必须是独立标记；出现在文件名、路径、URL、代码块或反引号中的 `chat` / `plan` / `dev` 不触发。
2. 同一条消息出现多个模式指令时，按 `dev` > `plan` > `chat` 执行。
3. `chat`：仅做分析、核验与建议。可读取本地文件、浏览网页、查询 Git、运行分析命令，并创建或清理临时分析产物；不得修改、删除、重命名现有项目文件。
4. `plan`：在 `chat` 基础上，可创建、修改、重命名、删除 `*.md` 文档；非 Markdown 的现有项目文件只读，不做真实实现。
5. `dev`：可修改项目文件并执行必要验证；遇到高风险或不可逆操作时，先征求用户确认。
6. `plan` 与 `dev` 的授权默认仅持续当前这一条消息，除非用户再次明确呼唤。

## 5. 文档维护触发
1. `更新开发日志` / `更新日志` / `记录最近改动` -> 更新 `doc/_devlog.md`，且必须追加，不得改写历史。
2. `更新项目说明` / `更新说明` / `同步项目文档` -> 先更新对应专题文档，再视需要同步 `doc/_instruction.md` 入口页与 `Instruction.md`。
3. `制定开发计划` -> 创建或更新 `plan/<yymmddhhmm>-<主题>-devplan.md`。
4. `评估开发结果` -> 创建或更新 `plan/<yymmddhhmm>-<主题>-devreport.md`。
5. 同一轮同时需要更新项目说明和开发日志时，先更新专题说明，再更新 `doc/_instruction.md`，最后更新 `doc/_devlog.md`。

## 6. `doc/_devlog.md` 规则
1. 只追加，不回改历史。
2. 以 Git 证据、文件差异和会话事实为依据，无法确认时明确标注推断来源。
3. 每条记录必须包含绝对时间。内容应简明扼要，建议用一两句话概括核心变更、关键文件及结果，避免过度复杂的模板。
4. 不记录未发生或未验证的修改，存在失败或回滚时必须说明原因与恢复措施。

## 7. `doc/_instruction.md` 规则
1. `doc/_instruction.md` 只保留入口信息，不再承载完整项目事实。
2. 需要更新当前项目事实时，优先更新 `doc/_instruction/core.md`、`doc/_instruction/structure.md`、`doc/_instruction/glossary.md`。
3. 若入口页内容变化，要同步维护阅读顺序、专题指向和更新时间。
4. 专题内容必须反映仓库当前真实状态，目录树和术语表都要基于实时扫描与可核验事实。
5. 更新时先核对真实结构，再合并近期变更，清理过期路径和废弃术语。
6. 目录树中的每个展示项都要附简短用途说明；无法验证时标注“待确认”。

## 8. 一致性与安全
1. 文档冲突时，以代码与 Git 证据为准。
2. 不在文档中暴露密钥、令牌、密码等敏感信息。
3. 无法验证的信息统一放入“待确认事项”，不要编造。

## 9. 完成标准
执行“更新项目说明/更新开发日志”后，必须同时满足：
1. 目标文档已成功更新或创建。
2. 内容与当前项目真实状态一致。
3. 包含绝对时间戳。
4. 可追溯到具体文件或提交依据。
5. 无虚构改动、无过期结构、无敏感信息明文扩散。
