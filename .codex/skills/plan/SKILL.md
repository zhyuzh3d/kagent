---
name: plan
description: 规划文档模式技能。仅在用户明确“呼唤 plan 模式”时使用：用户消息中出现独立的模式指令 `plan`（作为单独词/单独标记），例如“plan，请帮我撰写计划”。该模式允许撰写/更新文档（默认写入 `doc/` 目录，除非用户指定），只做需求分析、功能/技术方案设计、里程碑计划、任务拆解等纯文档输出，不执行真实编码实现。注意：不要因为子串匹配误触发（如“planning”、文件名、反引号内容等都不算）。
---

# Plan

## 目标
在不改动代码的前提下，产出高质量、可执行的项目规划与设计文档。

## 触发规则
1. 仅当用户消息中存在“明确模式指令” `plan` 时触发，且 `plan` 必须是独立标记：
   - `plan` 前一字符为消息开头，或为空白，或为中文/英文标点（如 `，,:：；。!?`）。
   - `plan` 后一字符为消息结尾，或为空白，或为中文/英文标点（如 `，,:：；。!?`）。
   - 特别地：`plan` 后一字符若为 `.` `/` `\\` `-` `_` 或字母/数字，视为“仍在标识符/路径/文件名中”，不算模式指令（例如 `plan.md`、`/plan`、`planning`、`plan_v2`）。
2. 允许形式（可在句首或句中）：`plan`、`plan，...`、`plan: ...`、`plan：...`、`... plan，...`。
3. 不允许形式（避免误触发）：`planning`、`plan.md`、`/plan`、代码块/反引号里的 `plan`、URL 中的 `plan`。
4. 若同一条消息里同时出现多个模式指令，按最高权限执行：`dev` > `plan` > `chat`。出现 `dev` 时，本 Skill 不应阻止编码与文件操作（仍可同时产出文档）。

## 执行范围
1. 需求澄清与功能边界定义。
2. 技术方案对比与架构草案。
3. 开发计划、里程碑、任务拆解、排期建议。
4. 验收标准、测试策略、风险清单、回滚预案。
5. 其他纯文档设计工作（PRD、技术设计、实施计划等）。
6. 允许创建/更新文档文件：
   - 默认写在 `doc/`，除非用户明确指定其他目录或文件名。
   - 例外：当用户意图为 `制定开发计划` 时，必须写入 `plan/<需求文档基名>-dev-plan.md`（与需求文档同名落地），而不是写入 `doc/`。

## 执行步骤
1. 先验证本条消息是否“明确呼唤 plan 模式”；若不是（例如只是出现子串或出现在代码/文件名中），立即停止并不应用本 Skill。
2. 去除模式指令 `plan`（以及紧随其后的 `:`/`：`/`,`/`，` 等标点），识别用户目标与约束，补齐关键假设。
3. 读取必要上下文（相关文档与代码结构）以保证方案贴合真实项目；默认优先读 `doc/_instruction.md` 和 `AGENTS.md`。
4. 输出结构化文档：背景、目标、范围、方案、任务分解、风险、验收标准。
5. 若用户未指定输出文件路径：
   - 一般情况：默认将本次文档写入 `doc/`（例如 `doc/plan.md` 或按主题命名）。
   - 当用户意图为 `制定开发计划`：默认将本次文档写入 `plan/<需求文档基名>-dev-plan.md`（例如需求文档为 `plan/T0-2026-03-03-01.md`，则写 `plan/T0-2026-03-03-01-dev-plan.md`）。
   - 仅在用户明确要求“只输出不落盘”时不写文件。
6. 若同一条消息里同时出现 `dev`，按最高权限进入 `dev` 模式，本 Skill 不应阻止编码与文件操作（仍可同时产出文档）。
7. 若用户请求“直接开发”但未呼唤 `dev`，先给可执行计划，并提示用户再次明确呼唤 `dev` 才进入开发执行。

## 硬性边界
1. 不执行编码实现，不修改业务代码（文档文件除外）。
2. 不运行构建、测试、部署等开发执行动作（纯文档工作除外，例如格式化 Markdown 不算构建）。
3. 不宣称“已实现”或“已修复”任何未实际落地的内容。

## 完成标准
1. 交付的规划/设计文档结构完整、可直接执行。
2. 文档明确范围、任务、风险和验收条件。
3. 本轮未发生实际编码实现与业务代码变更（允许新增/修改 `doc/` 下文档）。

## 权限持续性
`plan` 默认是一条消息内的一次性授权。下一条消息若未再次明确呼唤 `plan` 或 `dev`，默认回到 `chat` 权限。
